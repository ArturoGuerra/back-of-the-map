apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backofthemap.fullname" . }}-test-connection"
  labels:
{{ include "backofthemap.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "backofthemap.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
