apiVersion: v1
kind: Service
metadata:
  name: {{ include "backofthemap.fullname" . }}
  labels:
    {{- include "backofthemap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "backofthemap.selectorLabels" . | nindent 4 }}
